using UnityEngine;
using System.Collections;

public class TargetingSystem : MonoBehaviour 
{
	public Texture2D crosshairImage;

	public Texture aTexture;


	/*void OnGui()
	{
		//2d texture has is not showing up in, also look for sprite animation for the crosshair
		
		GUI.DrawTexture(new Rect(Screen.width/2, Screen.height/2, 10,10), aTexture, ScaleMode.ScaleToFit, true, 10.0F);

		//check position of cross hair
		GUILayout(new Rect(Screen.width/2, Screen.height/2, 10,10),crosshairImage);
		}*/
	 

	void playerDetected()
	{
	//crosshair turns red

		}//make another empty game object with another empty collider 

	//

	// Use this for initialization
	void Start () 
	{	
		//OnGui ();
	}

}
